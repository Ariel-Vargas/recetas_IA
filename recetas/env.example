# Django Configuration
# IMPORTANTE: Copia este archivo como .env y actualiza los valores

# Django Secret Key - CAMBIAR EN PRODUCCIÓN
# Genera una nueva con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-dev-key-CHANGE-THIS-IN-PRODUCTION

# Debug Mode (True solo en desarrollo, False en producción)
DEBUG=True

# Allowed Hosts (separados por coma, sin espacios)
# Ejemplo: localhost,127.0.0.1,midominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Para SQLite (por defecto): sqlite:///db.sqlite3
# Para PostgreSQL: postgresql://user:password@localhost:5432/dbname
DATABASE_URL=sqlite:///db.sqlite3

# CORS Settings (dominios permitidos, separados por coma)
# En desarrollo puedes usar http://localhost:3000
# En producción especifica tus dominios exactos
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Model Paths (rutas relativas al directorio recetas/)
# Si el modelo está en la raíz del proyecto: ../modelo_exportado
MODEL_PATH=../modelo_exportado

# Optional: Email configuration (para notificaciones)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-contraseña-de-aplicacion
